import styled from 'styled-components'

const Container = styled.div`
    position: relative;

    #container {
        height: 100vh;
        width: 100vw;
    }

    button.print {
        position: fixed;
        top: 0;
        right: 0;
    }
`

export default Container